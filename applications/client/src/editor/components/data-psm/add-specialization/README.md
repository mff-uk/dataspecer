# Add specialization components

Available for classes when the *inheritance OR* option is enabled. Wraps given class with OR (if not yet) and adds new specialized class that includes the previous one.
