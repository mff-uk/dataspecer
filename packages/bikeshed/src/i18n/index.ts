// To add a new language simply create a new import and add the language code into the list below
// Use ISO 639-1 codes if possible

import cs from "./cs.json";
import en from "./en.json";

export default {
    cs, en
}
