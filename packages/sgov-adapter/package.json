{
  "name": "@dataspecer/sgov-adapter",
  "version": "0.0.0",
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "prebuild": "node ./build/prebuild.mjs",
    "integration-test": "vitest run --pass-with-no-tests"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@dataspecer/core": "*",
    "js-string-escape": "^1.0.1"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "typescript": "^5.6.3",
    "vitest": "^3.1.1"
  }
}
