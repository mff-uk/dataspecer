import { TransitionsGenerator } from "../src/engine/transitions/transitions-generator"

test("", () => {
    //const x = new TransitionsGenerator().getNodeTransitions(null!, null!);

    expect("").not.toBeNull();
});
