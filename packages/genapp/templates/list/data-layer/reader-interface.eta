import { <%= it.return_type %> } from <%~ it.return_type_path %>;

export interface ListReader {
    readInstanceCount(): Promise<number | undefined>;
    readInstanceList<T>(request: T): Promise<<%= it.return_type %>>;
}
