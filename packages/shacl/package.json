{
  "name": "@dataspecer/shacl",
  "version": "0.0.0",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "prebuild": "rimraf lib",
    "test": "jest \"^(.*/)*(.*\\.)*(spec)\\.[jt]s$\" --passWithNoTests"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@dataspecer/core": "*",
    "n3": "^1.16.4"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.5.0",
    "rimraf": "^4.4.1",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.4"
  }
}
